"use client";

/**
 * Cookie / analytics consent banner. With external analytics removed,
 * this component is a no-op stub. It renders nothing but is kept so
 * existing layout imports don't break.
 */
export function CookieBanner() {
  return null;
}
